export interface Driver {
  id: string
  firstName: string
  lastName: string
  phoneNumber: number
  isActive: boolean
}

export interface DriverDto {
  firstName: string
  lastName: string
  phoneNumber: number
  isActive: boolean
}